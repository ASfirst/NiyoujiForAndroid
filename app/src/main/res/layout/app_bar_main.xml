<?xml version="1.0" encoding="utf-8"?>
<android.support.design.widget.CoordinatorLayout
        xmlns:android="http://schemas.android.com/apk/res/android"
        xmlns:app="http://schemas.android.com/apk/res-auto"
        xmlns:tools="http://schemas.android.com/tools"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        tools:context="com.jeramtough.niyouji.controller.activity.MainActivity">

    <LinearLayout
            android:layout_height="@dimen/top_navigation_height"
            android:layout_width="match_parent"
            android:theme="@style/AppTheme.AppBarOverlay"
            android:orientation="horizontal"
            android:background="@color/colorPrimaryDark">

        <android.support.v7.widget.Toolbar
                android:id="@+id/toolbar"
                android:layout_width="wrap_content"
                android:layout_height="match_parent"
                app:popupTheme="@style/AppTheme.PopupOverlay"/>

        <RadioGroup
                android:id="@+id/radioGroup_main_navigation"
                android:layout_width="0dp"
                android:layout_height="match_parent"
                android:layout_weight="1"
                android:orientation="horizontal">

            <RadioButton
                    android:id="@+id/radioButton_0"
                    android:layout_width="0dp"
                    android:layout_height="match_parent"
                    android:layout_weight="1"
                    android:button="@null"
                    android:text="广场"
                    android:gravity="center"
                    android:textSize="23sp"
                    android:textStyle="bold"
                    android:background="@drawable/radiobutton_top_menu"/>

            <RadioButton
                    android:id="@+id/radioButton_1"
                    android:layout_width="0dp"
                    android:layout_height="match_parent"
                    android:layout_weight="1"
                    android:button="@null"
                    android:text="直播"
                    android:gravity="center"
                    android:textSize="23sp"
                    android:textStyle="bold"
                    android:background="@drawable/radiobutton_top_menu"/>
        </RadioGroup>

        <ImageButton
                android:id="@+id/imageButton_perform"
                android:layout_width="60dp"
                android:layout_height="match_parent"
                android:background="@drawable/plane"/>

    </LinearLayout>

    <include layout="@layout/content_main"/>

</android.support.design.widget.CoordinatorLayout>
